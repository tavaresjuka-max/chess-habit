import './App.css';

export function App() {
  return (
    <main className="app-shell">
      <section className="app-intro" aria-labelledby="app-title">
        <h1 id="app-title">Rotina</h1>
        <p>Base P0 limpa para a rotina pessoal de treino.</p>
      </section>
    </main>
  );
}
